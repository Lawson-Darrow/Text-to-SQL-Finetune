"""Spider → prompt/target pairs (Milestone 3).

Serialize each example's DB schema (tables + columns) into the prompt alongside the
natural-language question; target is the gold SQL. Stub for now.
"""
